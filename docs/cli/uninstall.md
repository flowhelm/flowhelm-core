---
summary: "CLI reference for `flowhelm uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `flowhelm uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
flowhelm uninstall
flowhelm uninstall --all --yes
flowhelm uninstall --dry-run
```
