---
summary: "CLI reference for `flowhelm clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `flowhelm clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `flowhelm clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `flowhelm clawbot qr` (same behavior as [`flowhelm qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `flowhelm clawbot qr` -> `flowhelm qr`
